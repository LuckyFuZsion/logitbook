rules_version = '2';
// All access via Firebase Admin SDK only (server). Deny client reads/writes unless you add auth rules later.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
